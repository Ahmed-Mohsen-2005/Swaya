import { useEffect,useState } from 'react';import { dataService } from '../../services/dataService';import { Card } from '../../components/ui/Card';import { StatusBadge } from '../../components/ui/StatusBadge';
export default function TeacherReports(){const [reports,setReports]=useState([]);useEffect(()=>{dataService.getReportsForRole('teacher').then(setReports)},[]);return <Card><div className="section-title"><h2>Reports</h2><div className="filters"><button className="pill active">All</button><button className="pill">Session</button><button className="pill">Weekly</button></div></div><div className="grid grid-3">{reports.map(r=><div className="student-card" key={r.id}><h3>{r.title}</h3><p className="muted">{r.summary}</p><StatusBadge status="stable">{r.status}</StatusBadge></div>)}</div></Card>}
