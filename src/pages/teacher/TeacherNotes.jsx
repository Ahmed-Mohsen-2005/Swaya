import { useEffect,useState } from 'react';import { Card } from '../../components/ui/Card';import { Button } from '../../components/ui/Button';import { notes } from '../../data/mockData';
export default function TeacherNotes(){return <div className="grid grid-2"><Card><h2>Add Session Note</h2><div className="grid"><select className="select"><option>Abdullah Ali</option><option>Noor A.</option></select><textarea placeholder="Write teacher observation..." rows={7}/><Button>Save Note</Button></div></Card><Card><h2>Recent Notes</h2>{notes.filter(n=>n.authorRole==='teacher').map(n=><div className="student-card" key={n.id}><b>{n.importance.toUpperCase()}</b><p>{n.content}</p><span className="small muted">{new Date(n.createdAt).toLocaleString()}</span></div>)}</Card></div>}
