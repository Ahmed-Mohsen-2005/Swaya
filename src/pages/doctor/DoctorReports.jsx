import { reports } from '../../data/mockData';import { Card } from '../../components/ui/Card';import { Button } from '../../components/ui/Button';import { StatusBadge } from '../../components/ui/StatusBadge';
export default function DoctorReports(){return <div className="grid grid-2"><Card><h2>Create Report</h2><div className="grid"><select className="select"><option>Abdullah Ali</option><option>Noor A.</option></select><select className="select"><option>Clinical</option><option>Parent-safe</option><option>Teacher</option></select><textarea rows={7} placeholder="Clinical summary and recommendations..."/><Button>Create Report</Button></div></Card><Card><h2>Existing Reports</h2>{reports.map(r=><div className="student-card" key={r.id}><b>{r.title}</b><p className="muted">{r.summary}</p><StatusBadge status={r.status==='published'?'stable':'warning'}>{r.status}</StatusBadge></div>)}</Card></div>}
