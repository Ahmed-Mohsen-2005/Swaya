import { useParams } from 'react-router-dom';import { therapyPlans, students } from '../../data/mockData';import { Card } from '../../components/ui/Card';import { Button } from '../../components/ui/Button';
export default function DoctorTherapyPlans(){const {studentId}=useParams();const plans=studentId?therapyPlans.filter(p=>p.studentId===studentId):therapyPlans;return <div className="grid">{plans.map(p=>{const s=students.find(x=>x.id===p.studentId);return <Card key={p.id}><div className="section-title"><h2>{s?.fullName} Therapy Plan</h2><span className="badge green">{p.status}</span></div><div className="form-row"><label>Robot Style<select className="select" defaultValue={p.robotInteractionStyle}><option>gentle</option><option>balanced</option><option>active</option></select></label><label>Sensory Level<select className="select" defaultValue={p.sensoryLevel}><option>low</option><option>medium</option><option>high</option></select></label></div><h3>Goals</h3><div className="grid grid-2">{p.goals.map(g=><div className="student-card" key={g.id}><b>{g.title}</b><p className="muted">{g.category} · {g.progress}% progress</p></div>)}</div><h3>Classroom Strategies</h3><textarea rows={4} defaultValue={p.classroomStrategies.join('\n')} style={{width:'100%'}}/><h3>Home Strategies</h3><textarea rows={3} defaultValue={p.homeStrategies.join('\n')} style={{width:'100%'}}/><Button style={{marginTop:14}}>Save Plan</Button></Card>})}</div>}
