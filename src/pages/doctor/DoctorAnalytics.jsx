import { Card } from '../../components/ui/Card';import { LiveLineChart, StudentsBarChart } from '../../components/charts/SimpleCharts';import { students } from '../../data/mockData';
const data=[0,1,2,3,4,5,6,7].map((_,i)=>({time:`D${i+1}`,attention:60+i*2,engagement:56+i*3,stress:56-i*2}));
export default function DoctorAnalytics(){return <div className="grid"><Card><div className="section-title"><h2>Patient Analytics</h2><div className="filters"><button className="pill active">Stress</button><button className="pill">Attention</button><button className="pill">Intervention Response</button></div></div></Card><div className="grid grid-2"><Card><h2>Behavior Pattern Trend</h2><LiveLineChart data={data}/></Card><Card><h2>Assigned Patient Comparison</h2><StudentsBarChart data={students.filter(s=>s.doctorIds.length).map(s=>({name:s.shortName,...s.baselineMetrics}))}/></Card></div></div>}
